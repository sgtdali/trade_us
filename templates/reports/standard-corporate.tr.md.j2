{%- set L = lookup -%}
# {{ ticker }} {{ period }} Temel Analiz Raporu

*Bu rapor `fundamental-pipeline` tarafından yapılandırılmış verilerden otomatik olarak üretilmiştir. Yatırım tavsiyesi, hedef fiyat veya alım/satım/tut önerisi içermez.*

## 1. Yönetici Özeti

{{ ticker }} için {{ period }} dönemi temel analiz çıktısı {{ signals|length }} sinyal, {{ risks|length }} risk kaydı ve {{ summaries|length }} özet bölümünden oluşur. Genel finansal görünüm (`profitability_summary`) durumu: **{{ summaries.get('profitability_summary', {}).get('status', 'n/a') }}**. Bu rapor bir sentezdir; nihai değerlendirme için ilgili bölümlerdeki sinyal ve risk detaylarına bakınız.

## 2. Kullanılan Kaynaklar

{% if sources %}
| Kaynak ID | Dosya | Tür | Birincil mi |
|---|---|---|---|
{% for s in sources -%}
| {{ s.source_id }} | {{ s.path }} | {{ s.source_type }} | {{ 'Evet' if s.is_primary else 'Hayır' }} |
{% endfor %}
{% else %}
Kaynak manifesti bulunamadı.
{% endif %}

## 3. Kapsam ve Kısıtlar

Bu rapor yalnızca yukarıdaki kaynak belgelerden çıkarılan yapılandırılmış verilere dayanır. Tahmin veya varsayımla doldurulmuş değer içermez; eksik veriler "Eksik Veriler" bölümünde ayrıca listelenir. Rapor bir değerleme, hedef fiyat veya yatırım tavsiyesi üretmez.

## 4. Şirket ve İş Modeli

- **Unvan:** {{ company_name or 'n/a' }}
- **Şirket tipi:** {{ company_type }} / **Sektör modülü:** {{ sector_module }}
- **Resmi sektör:** {{ classification.get('official_sector') or 'n/a' }}
- **Resmi faaliyet konusu:** {{ classification.get('official_industry') or 'n/a' }}
{% if classification.get('actual_activity') %}
- **Faaliyet açıklaması:** {{ classification.actual_activity }}
{% endif %}

## 5. Gelir Bileşimi

{% if L.is_available('revenue_passenger_total') or L.is_available('revenue_cargo_total') %}
| Kalem | Değer |
|---|---|
{% for mid, name in [('revenue_passenger_total','Yolcu Geliri'), ('revenue_cargo_total','Kargo Geliri'), ('revenue_technical','Teknik Gelir'), ('revenue_other','Diğer Gelir'), ('revenue_total','Toplam Gelir')] -%}
{% if L.is_available(mid) -%}
| {{ name }} | {{ L.fmt(mid) }} |
{% endif -%}
{% endfor %}
{% else %}
Gelir kırılımı bu dönem için mevcut değil; yalnızca toplam gelir raporlanmıştır: {{ L.fmt('revenue_total') }}.
{% endif %}

## 6. Finansal Performans Özeti

| Kalem | Bu Dönem | Önceki Dönem |
|---|---|---|
| Toplam Gelir | {{ L.fmt('revenue_total') }} | {{ L.fmt_comparison('revenue_total') }} |
| Brüt Kâr | {{ L.fmt('gross_profit') }} | {{ L.fmt_comparison('gross_profit') }} |
| Faaliyet Kârı (yatırım dahil) | {{ L.fmt('operating_profit') }} | {{ L.fmt_comparison('operating_profit') }} |
| Net Dönem Kârı | {{ L.fmt('net_profit_for_period') }} | {{ L.fmt_comparison('net_profit_for_period') }} |

## 7. Gelir Büyümesi

{% for mid, name in [('revenue_growth','Toplam Gelir'), ('passenger_revenue_growth','Yolcu Geliri'), ('cargo_revenue_growth','Kargo Geliri'), ('technical_revenue_growth','Teknik Gelir')] -%}
{% if L.is_available(mid) -%}
- **{{ name }} Büyümesi:** {{ L.fmt(mid) }}
{% endif -%}
{% endfor %}

## 8. Brüt Kârlılık

Brüt kâr {{ L.fmt('gross_profit') }}, brüt kâr marjı {{ L.fmt('gross_margin') }} ({{ L.change('gross_margin') }}). {% if signals.get('gross_margin_trend') %}{{ signals.gross_margin_trend.reason }}{% endif %}

## 9. Yatırım Faaliyetleri Öncesi Faaliyet Sonucu

Yatırım faaliyetleri öncesi faaliyet sonucu {{ L.fmt('operating_profit_before_investment_activities') }}, marjı {{ L.fmt('operating_margin_pre_investment') }}. {% if signals.get('operating_profit_before_investment_trend') %}{{ signals.operating_profit_before_investment_trend.reason }}{% endif %}

## 10. Yatırım Kalemleri

| Kalem | Değer |
|---|---|
| Yatırım Geliri | {{ L.fmt('investment_income') }} |
| Yatırım Gideri | {{ L.fmt('investment_expense') }} |
| Özkaynak Yöntemi Yatırım Kâr Payı | {{ L.fmt('equity_method_investment_profit_share') }} |

## 11. IFRS Faaliyet Kârı (Yatırım Kalemleri Dahil)

Faaliyet kârı (IFRS "Faaliyet Kârı" satırı) {{ L.fmt('operating_profit') }}, marjı {{ L.fmt('operating_margin') }}. {% if signals.get('operating_profit_including_investments_trend') %}{{ signals.operating_profit_including_investments_trend.reason }}{% endif %}

## 12. Net Kâr

Net dönem kârı {{ L.fmt('net_profit_for_period') }}, net kâr marjı {{ L.fmt('net_margin') }}. {% if signals.get('net_profit_trend') %}{{ signals.net_profit_trend.reason }}{% endif %}

## 13. Bilanço

| Kalem | Değer |
|---|---|
| Toplam Varlıklar | {{ L.fmt('total_assets') }} |
| Toplam Yükümlülükler | {{ L.fmt('total_liabilities') }} |
| Toplam Özkaynak | {{ L.fmt('total_equity') }} |

## 14. Likidite

Cari oran {{ L.fmt('current_ratio') }} ({{ L.change('current_ratio') }}); nakit + kısa vadeli yatırım / kısa vadeli yükümlülük oranı {{ L.fmt('cash_and_st_investments_to_current_liabilities') }}. {% if signals.get('liquidity_trend') %}{{ signals.liquidity_trend.reason }}{% endif %}

## 15. Finansal Borç

Finansal borç (kira hariç) {{ L.fmt('financial_debt') }}; Borç/Özkaynak {{ L.fmt('debt_to_equity') }}.

## 16. Kira Yükümlülükleri

Toplam kira yükümlülüğü {{ L.fmt('lease_liability_total') }}. {% if signals.get('lease_burden_trend') %}{{ signals.lease_burden_trend.reason }}{% endif %}

## 17. Nakit Akışı

Net işletme faaliyetlerinden nakit akışı {{ L.fmt('cf_net_operating_activities') }}. {% if signals.get('operating_cash_flow_trend') %}{{ signals.operating_cash_flow_trend.reason }}{% endif %}

## 18. Yatırım Harcamaları (Capex)

Nakit capex {{ L.fmt('cf_capex_ppe_intangible') }}; gelire oranı {{ L.fmt('capex_to_revenue') }}.

## 19. Serbest Nakit Akışı

Standart FCF (işletme nakit akışı + işaretli capex) {{ L.fmt('free_cash_flow') }}; kira ödemeleri sonrası alternatif FCF {{ L.fmt('free_cash_flow_after_lease_payments') }}. {% if signals.get('free_cash_flow_trend') %}{{ signals.free_cash_flow_trend.reason }}{% endif %}

## 20. Getiri Metrikleri

{% if L.is_available('roa') or L.is_available('roe') or L.is_available('roic') %}
| Metrik | Değer |
|---|---|
| ROA | {{ L.fmt('roa') }} |
| ROE | {{ L.fmt('roe') }} |
| ROIC | {{ L.fmt('roic') }} |
{% else %}
Getiri metrikleri (ROA/ROE/ROIC) bu dönem için hesaplanmamıştır; bu metrikler yalnızca yıllık (FY) dönemler için, tekil çeyreğin yıllıklaştırılması yapılmadan üretilir.
{% endif %}

## 21. Temel Riskler

{% for r in risks -%}
- **{{ r.title }}** ({{ r.severity }}, {{ r.direction }}): {{ r.description }}
{% endfor %}

## 22. Olumlu Gelişmeler

{% for s in positive_signals -%}
- **{{ s.signal_id }}**: {{ s.reason }}
{% endfor %}
{% if contextual_signals %}

### Bağlam Gerektiren Göstergeler

Bu sinyallerin ham yönü korunmuştur; ancak mevcut seviye veya veri tamlığı nedeniyle koşulsuz olumlu olarak sınıflandırılmamıştır.

{% for s in contextual_signals -%}
- **{{ s.signal_id }}**: {{ s.reason }} **Bağlam:** {{ s.context.reason }} (ham: `{{ s.value }}`, sunum: `{{ s.context.presentation_status }}`)
{% endfor %}
{% endif %}

## 23. Olumsuz Gelişmeler

{% for s in negative_signals -%}
- **{{ s.signal_id }}**: {{ s.reason }}
{% endfor %}

## 24. İzlenecek Metrikler

{% for m in unavailable_derived -%}
- `{{ m.metric_id }}`: {{ m.reason or 'veri mevcut değil' }}
{% endfor %}

## 25. Eksik Veriler

{% for s in unavailable_signals -%}
- `{{ s.signal_id }}`: {{ s.reason }}
{% endfor %}
{% if not unavailable_signals and not unavailable_derived %}
Bu dönem için işaretlenmiş eksik sinyal veya oran bulunmamaktadır.
{% endif %}

## 26. Değerleme için Eksik Dış Veriler

Bu rapor aşağıdaki dış verileri kullanmaz ve içermez: güncel piyasa fiyatı, dolaşımdaki pay sayısı, peer/benzer şirket verisi, analist konsensüsü, risksiz faiz oranı, risk primi. Bu veriler olmadan F/K, FD/FAVÖK, PD/DD veya hedef fiyat hesaplanamaz.

## 27. Sonuç

Bu rapor {{ ticker }} için {{ period }} dönemine ait yapılandırılmış temel analiz verilerinin bir sentezidir. {{ positive_signals|length }} pozitif ve {{ negative_signals|length }} negatif sinyal tespit edilmiştir. Rapor bir alım, satım veya tutma tavsiyesi değildir.

## 28. Yasal Uyarı

Bu belge otomatik olarak üretilmiş, yalnızca bilgilendirme amaçlı yapılandırılmış bir temel analiz çıktısıdır. **Yatırım tavsiyesi değildir.** Hiçbir hisse senedi için alım, satım veya tutma önerisi içermez ve hedef fiyat veya değerleme sonucu üretmez. Yatırım kararları bağımsız araştırma ve profesyonel danışmanlık ile verilmelidir.
{% block aviation_section %}{% endblock %}
